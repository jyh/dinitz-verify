name = "dinitz-verify"
defaultTargets = ["Challenge", "Submission"]

[[require]]
name = "mathlib"
git = "https://github.com/leanprover-community/mathlib4"
rev = "360da6fa66c1273b76b6b2d8c5666fd5ac2e3b56"

# The statement: imports only Mathlib, proves nothing.
[[lean_lib]]
name = "Challenge"

# The proof: imports `Challenge`, so the statement is shared, not restated.
[[lean_lib]]
name = "Submission"
